name: snaketest
channels:
    - conda-forge
    - bioconda
    - defaults
dependencies:
    - r-base
    - r-irkernel=1.1
    - r-devtools
    - snakemake-minimal=6.3.0
    - mamba
    - bwa
    - samtools=1.10
    - bcftools
